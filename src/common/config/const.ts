export enum SERVICE_CONFIGS {
  NAME = 'service.name',
  DESCRIPTION = 'service.description',
  VERSION = 'service.version',
}

export enum SERVICE_URL_CONFIGS {
  BASE = 'service.url.base',
  DOCS = 'service.url.docs',
}

export enum SERVER_CONFIGS {
  HOST = 'server.host',
  PORT = 'server.port',
}

export enum SECURITY_DOCS_CONFIGS {
  USERNAME = 'security.docs.username',
  PASSWORD = 'security.docs.password',
}

export enum DATABASE_CONFIGS {
  DIALECT = 'database.dialect',
  HOST = 'database.host',
  PORT = 'database.port',
  USERNAME = 'database.username',
  PASSWORD = 'database.password',
  DATABASE = 'database.database',
}

export enum SERVICES_CONFIGS {
  FRAUD_AWAY_PATH = 'services.fraudAway.path',
  FRAUD_AWAY_TIMEOUT = 'services.fraudAway.timeout',
  SIMPLE_FRAUD_PATH = 'services.simpleFraud.path',
  SIMPLE_FRAUD_TIMEOUT = 'services.simpleFraud.timeout',
}

export enum LOGGER_CONFIGS {
  HTTP = 'logger.http',
}
